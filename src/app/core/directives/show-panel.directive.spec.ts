import { ShowPanelDirective } from './show-panel.directive';

describe('ShowPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPanelDirective();
    expect(directive).toBeTruthy();
  });
});
